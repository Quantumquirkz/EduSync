# EduSync - Configuración de Spring Boot (application.properties)
#
# Este archivo contiene la configuración de la aplicación backend de EduSync.
# Define la conexión a la base de datos PostgreSQL (Supabase) y otras
# configuraciones del framework Spring Boot.
#
# IMPORTANTE: En producción, estas credenciales deben estar en variables
# de entorno y no hardcodeadas en el archivo de configuración.
#
# @author EduSync Team
# @version 1.0.0

# Configuración de la base de datos PostgreSQL (Supabase)
# URL de conexión a la base de datos con parámetros de seguridad
spring.datasource.url=jdbc:postgresql://aws-0-us-east-2.pooler.supabase.com:6543/postgres?sslmode=require

# Credenciales de acceso a la base de datos
# Usuario de la base de datos PostgreSQL
spring.datasource.username=postgres.faollalzdyoigzfzggwy

# Contraseña de la base de datos PostgreSQL
# IMPORTANTE: En producción, usar variables de entorno
spring.datasource.password=wMVC0xE9QbmPTpqx

# Driver de PostgreSQL para la conexión JDBC
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de inicialización de base de datos
# 'never' significa que no se ejecutarán scripts de inicialización automáticamente
spring.sql.init.mode=never